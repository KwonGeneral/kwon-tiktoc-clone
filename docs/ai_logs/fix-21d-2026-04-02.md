# Fix 21D: 프로필 + 팔로잉 필터링

- **날짜**: 2026-04-02
- **범위**: 프로필 썸네일, 게시 후 내 영상, 팔로잉 탭

## 작업 내용

### 1. 프로필 영상 썸네일 표시 수정
- `profile_video_grid.dart`의 `_VideoThumbnail`을 `StatefulWidget`으로 변환
- `VideoPlayerController.networkUrl()`로 첫 프레임을 표시
- `AutomaticKeepAliveClientMixin`으로 스크롤 시 재초기화 방지

### 2. 게시 후 프로필 "내 영상" 탭에 영상 표시
- `FeedNotifier`에 `addUploadedVideo()` 메서드 추가
- `userId = 'current_user'`로 로컬 상태에 영상 직접 추가
- `publish_page.dart`에서 성공 시 `addUploadedVideo()` 호출 (기존 `invalidate` 대체)

### 3. 팔로잉 탭 영상 필터링 수정
- `_loadInitial()`에서 전체 영상 로드 (모든 페이지 순회)
- 기존: page 0만 로드 (10개) → 수정: 전체 13개 로드
- 팔로잉 필터가 모든 영상에 적용 가능

## 변경 파일
- `lib/presentation/profile/widget/profile_video_grid.dart`
- `lib/presentation/feed/provider/feed_provider.dart`
- `lib/presentation/publish/view/publish_page.dart`

## AI 활용
- Claude Code: 코드 분석 + 구현 전체
- Explore Agent: 3개 이슈 원인 조사
