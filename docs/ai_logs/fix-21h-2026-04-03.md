# Fix 21H: 메모리 누수 + 탭바 + 팔로잉 + 알림 + 앱 복원

- **날짜**: 2026-04-03
- **작업 시간**: ~1 세션

## 작업 내용

### CRITICAL — 앱 크래시
1. **OutOfMemoryError 수정**: VideoPlayerManager가 `videos`(전체) 대신 `displayVideos`(필터된 목록)를 사용하도록 수정. 탭 전환 시 `_disposeAll()` 호출하여 모든 컨트롤러 해제. 프리로딩 범위 ±2 → ±1로 축소.
2. **팔로잉 탭 같은 영상 반복**: 위와 동일 원인 — displayVideos 기준 인덱싱으로 해결.
3. **HLS 404 MP4 fallback**: `_initController`를 `_tryInitController`로 분리, HLS 실패 시 MP4 URL로 자동 재시도.

### HIGH — UX
4. **탭바 상단 고정**: 메인 Stack에 `Positioned(top: 0)` + `TopTabBar` 추가.
5. **알림 카테고리 탭 동작**: `_NotificationFilter` enum 도입, 카테고리 탭 시 해당 타입으로 필터링 토글.
6. **앱 즉시 복원**:
   - Android/iOS 네이티브 스플래시 배경을 검정(#000000)으로 변경
   - SharedPreferences에 lastVideoIndex + lastVideoThumbnailUrl 저장
   - 앱 시작 시 저장된 thumbnailUrl을 전체화면으로 즉시 표시 (로딩 중)

### MEDIUM — 코드 품질
7. **dart analyze 0개**: 미사용 필드/import 제거.

## 주요 변경 파일
- `lib/presentation/feed/provider/video_player_manager.dart` — displayVideos 사용, 탭 전환 dispose, ±1 범위, HLS fallback
- `lib/presentation/feed/view/feed_page.dart` — TopTabBar 추가, 썸네일 즉시 표시, 저장된 인덱스 복원
- `lib/presentation/feed/provider/feed_provider.dart` — 인덱스/썸네일 저장, 인덱스 복원
- `lib/presentation/notifications/view/notifications_page.dart` — 카테고리 필터링
- `lib/domain/repository/local_storage_repository.dart` — lastVideoIndex, lastVideoThumbnailUrl 추가
- `lib/data/datasource/local_storage_service.dart` — 위 인터페이스 구현
- `android/app/src/main/res/drawable*/launch_background.xml` — 검정 배경
- `ios/Runner/Base.lproj/LaunchScreen.storyboard` — 검정 배경

## AI 활용
- Claude Code: 전체 버그 분석 + 구현 + 정적 분석
- 사용자: 버그 목록 작성 + 테스트 영상(id 13~18) 서버 삭제
