# Fix 21K - 댓글 오버플로우 + 프로필 숫자 + 알림 검색 제거 + 추가 버그 수정

- **날짜**: 2026-04-03
- **브랜치**: `fix/phase-21k-overflow-stats-search`

## 주요 작업

### 계획된 Fix 21K 항목
1. **댓글 Column 오버플로우** — `Align` → `OverflowBox`, `LayoutBuilder`로 실제 가용 높이 사용, `AnimatedBuilder`로 성능 개선
2. **프로필 숫자 실제 데이터** — `currentUserProvider`에서 followedUserIds.length, isLiked 영상 수, Mock 팔로워 8명 계산
3. **알림 페이지 검색 제거** — AppBar actions 비움
4. **프로필 영상 탭 → 피드 매핑** — 추천탭 전환 + 정확한 index 이동

### 추가 수정 사항 (세션 중 발견/요청)
5. **댓글 시 영상 전체 표시** — `BoxFit.cover` → `BoxFit.contain`
6. **댓글 사용자 아바타** — VideoRemoteDataSource + MockVideoDataSource에 pravatar.cc URL 추가
7. **내 프로필 이미지** — comment_bottom_sheet에서 `currentUserProvider` 아바타 로드
8. **미구현 기능 숨김** — 댓글 입력 필드의 @ / emoji 아이콘 제거
9. **좋아요/싫어요 가로 정렬** — Column → Row, 각 아이콘 아래 숫자, 0 표시
10. **프로필 동영상 아이콘** — 우상단 `Icons.videocam` 추가
11. **프로필 그리드 통합** — 비디오+이미지를 createdAt 기준 단일 그리드
12. **영상 URL 불일치 감지** — VideoPlayerManager에서 controller dataSource 체크
13. **내 댓글 아바타/닉네임** — addComment에 currentUserProvider 연동, 로컬 저장/로드에 userAvatarUrl 추가
14. **답글에 답글 버튼 제거** — `isReply` 조건으로 답글 버튼 숨김
15. **사진 EXIF 방향 정규화** — `flutter_image_compress` 추가, 업로드 전 autoCorrectionAngle
16. **카메라 사진 모드 제거** — CameraMode.photo, _PhotoCaptureButton, _CameraModeSwitcher 삭제
17. **카메라 UI 리디자인** — 틱톡 스타일 (빨간 원 버튼 + 프로그래스 링 + X/✓ 버튼)
18. **카메라 프로그래스 부드럽게** — Timer 기반 → AnimationController(60fps) 기반
19. **카메라 전환 수정** — 기존 컨트롤러 먼저 dispose 후 새 컨트롤러 생성 + null 체크
20. **영상 삭제 후 무한 로딩** — PageView ValueKey + generation 카운터 + forceReinitialize
21. **HLS isBuffering 고정** — `isBuffering && !isPlaying` 조건
22. **VideoCard ValueListenableBuilder** — 컨트롤러 상태 변화 실시간 감지
23. **VideoPlayerManager race condition** — _generation 카운터로 이전 세대 비동기 작업 무효화
24. **UUID 호환 댓글 생성** — int.tryParse → hashCode.abs()

## 핵심 교훈
- PageView에 ValueKey를 부여하지 않으면 PageController를 새로 만들어도 이전 스크롤 위치를 기억함
- HLS 스트리밍에서 isBuffering이 true로 고정되는 video_player 플러그인 이슈
- VideoPlayerController의 상태 변화는 Riverpod state emission과 별개 → ValueListenableBuilder 필요
- 비동기 초기화의 race condition은 generation 카운터로 해결
